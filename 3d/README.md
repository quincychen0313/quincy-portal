# 3D Logo 素材

本資料夾只存放首頁所需的 3D 模型、JavaScript、CSS 與 Three.js。
首頁仍是根目錄原本的 `index.html`；打開原網址即可看到 3D Logo。

上傳方式：
1. 使用壓縮檔中的 `index.html` 覆蓋原本的首頁。
2. 將 `3d` 資料夾加入網站根目錄。
3. 其他原有檔案的位置、名稱與內容不需更動。

3D Logo 預設靜止。可拖曳旋轉，按播放後才會自動旋轉。
