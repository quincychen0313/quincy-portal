# Quincy Portal

這是可直接部署至 GitHub Pages 的靜態網站版本。

## 上傳方式

1. 將壓縮檔內的所有檔案放在 GitHub repository 的根目錄。
2. 在 repository 開啟 `Settings` → `Pages`。
3. 將來源設為 `Deploy from a branch`。
4. 選擇 `main` 分支與 `/(root)`，然後儲存。

網站需要連線至網路，才能載入動態背景與網路字型。
